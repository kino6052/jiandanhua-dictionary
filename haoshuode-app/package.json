{
  "name": "haoshuode-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-word-usage.js && vite build && node scripts/generate-pages.js && vite build && node scripts/generate-dictionary.js",
    "preview": "vite preview",
    "test": "bun test",
    "word-usage": "node scripts/generate-word-usage.js"
  },
  "dependencies": {
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0",
    "vite": "^6.0.0",
    "yaml": "^2.9.0"
  }
}
