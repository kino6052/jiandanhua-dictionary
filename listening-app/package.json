{
  "name": "listening-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun run build:manifest && vite build",
    "preview": "vite preview",
    "build:manifest": "bun run ./scripts/build-manifest.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@preact/signals": "^1.3.0",
    "htm": "^3.1.1",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
